from setuptools import setup

setup(
    name='my-python-lambda-app',
    version='0.1',
    description='A simple Python Lambda application',
    packages=['src'],
    install_requires=[
        # List your dependencies here
    ],
)
